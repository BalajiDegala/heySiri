[project]
name = "heysiri"
version = "0.1.0"
description = "Django website for team planning and tracking."
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "django==4.2.30",
    "gunicorn>=23.0.0,<24",
    "whitenoise>=6.11.0,<7",
]

[tool.uv]
package = false
